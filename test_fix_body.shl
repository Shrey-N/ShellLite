define say_hello to:
    print "Inside function"
    
say_hello:
    print "Body executed"

# Also test kwargs still work
# python import os
# print os.name 
# This was failing because kwargs is dict? No kwargs is list of tuples.
# I just fixed parser to use body=... so kwargs is default (None).
# interpreter.py checks `if node.kwargs`. None is falsey. Safe.
